# Kimi Tin — Organist

Next.js (TypeScript) starter for the personal artist site.

Quick start:

1. npm install
2. npm run dev

Notes:
- Pages Router is used (see `pages/`).
- SCSS with BEM conventions; global styles in `styles/`.
- Put your hero image at `public/images/hero.jpg`.
